[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "structure-generation-capability"
version = "0.3.1"
description = "AI-callable, token-efficient, vanilla-first, mod-aware Minecraft structure generation, auditing, modular layout, content, snapshotting, and review API"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
structure-capability = "structure_capability.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
