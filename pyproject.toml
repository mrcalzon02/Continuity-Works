[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "structure-generation-capability"
version = "0.3.2"
description = "AI-callable, token-efficient, public-serviceable, vanilla-first, mod-aware Minecraft structure generation and content API"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
structure-capability = "structure_capability.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
