Continuity Works Capability API v0.3.2
Validation date: 2026-09-09
=====================================

RESULT: AUTHORITATIVE HERO-SPEC BIOME CANDIDATE SOURCE WIRED THROUGH PRODUCTION ROUTING; EXPLICIT PROFILE VOCABULARIES VALIDATED; PROSE-ONLY PROFILE SECTIONS FAIL CLOSED; WHOLE-SOURCE/RENDERED NAMING GATES PASS; EXACT EXECUTABLE HEAD DEPLOYED THROUGH GITHUB PAGES; REMOTE JAVA DECISION AUTHORITY STILL NOT ATTACHED OR VERIFIED

Authority and accepted evidence
-------------------------------
Authoritative repository: mrcalzon02/Continuity-Works
Authoritative branch: main
Cycle starting head: 41dd27ff5b88c3f5ec14e37060634f4af9bb308a
Executable implementation/test head: ee3a0e9b83959810f16256ed05699b02a2bf14be
Successful exact-head validation run: https://github.com/mrcalzon02/Continuity-Works/actions/runs/34340512515
Successful exact-head GitHub Pages build/deployment: https://github.com/mrcalzon02/Continuity-Works/actions/runs/34340511569

Reconciliation and selected work
--------------------------------
The live repository reconciled at 41dd27ff5b88c3f5ec14e37060634f4af9bb308a. The catalog candidate source and production decision-authority bootstrap were already present and validated. The highest dependency-valid repository gap was the required B biome/environment ARCHETYPE_PROFILE candidate source.

Inspection of the authoritative hero corpus found two legitimate source shapes:
- some hero specifications expose biome/environment profiles as explicit level-three headings;
- some later specifications expose explicit bold bullet profile labels;
- some older specifications discuss biome/environment adaptation only as prose.

The implementation deliberately does not interpret prose into invented candidate identities. Only explicit profile labels are admitted as machine-facing vocabulary; prose-only specifications fail closed until their existing semantic content is normalized into explicit authoritative profile labels.

Implemented candidate authority
-------------------------------
EraStructureBiomeCandidateSource now supplies B candidates from the hero specification associated with the already-selected A archetype.

The source:
- requires mutator B and an existing selected archetype A;
- accepts only safe catalog identifiers before filesystem resolution;
- resolves exactly one sibling hero specification whose filename begins with the selected catalog ID;
- extracts only explicit profile labels inside the biome/environment adaptations section;
- accepts level-three profile headings and bold bullet profile labels;
- canonicalizes those explicit labels into decision-wire-safe uppercase underscore identifiers;
- preserves source ordering;
- rejects duplicate canonical labels;
- rejects missing sections, missing/multiple hero files, missing archetype state, and prose-only sections;
- derives sourceVersion from SHA-256 of the exact hero-specification bytes so any authority snapshot change invalidates derived dictionaries.

RoutingBlueprintDecisionCandidateSource now routes dynamic candidate requests by mutator code without adding transport or decision semantics. Production bootstrap composes:
- A -> EraStructureCatalogCandidateSource backed by the authoritative hero ledger;
- B -> EraStructureBiomeCandidateSource backed by the selected sibling hero specification.

The HTTP authority and Python publication bridge remain unchanged. Future C/Q/K sources can be added to the routing table without rewriting transport, dictionary integrity, mutation, validation, or generation logic.

Observed Java 17 behavior
-------------------------
The focused regression compiles the complete Continuity Works Java API module under Java 17 and runs against the actual repository hero authority.

Observed behavior verifies:
- E01-001 explicit level-three environment profiles produce ordered semantic candidates TEMPERATE_FOREST, BOREAL_COLD, TUNDRA_ALPINE, SAVANNA_DRY_GRASSLAND, ARID_DESERT, TROPICAL_HUMID, and COASTAL;
- candidate source identity is bound to the exact E01-001 hero-spec snapshot;
- a compact local dictionary code resolves back to BOREAL_COLD before authoritative BlueprintDecisionChain mutation;
- E01-017 explicit bold environment labels produce TEMPERATE, BOREAL, TUNDRA, SAVANNA, ARID, TROPICAL, and COASTAL_TRANSITION;
- E01-010 currently contains biome/environment authority only as narrative prose and is rejected rather than converted into guessed profile identifiers;
- B discovery without selected A is rejected;
- production routing continues to expose all 17 materialized A catalog candidates and routes B to the selected hero spec;
- unconfigured future profile mutators such as C fail closed;
- a byte change to the authoritative hero spec changes the source fingerprint even when explicit profile values remain unchanged.

Candidate integrity contract
----------------------------
The established compact candidate contract remains unchanged:
- source identity derives from the exact authority snapshot;
- dictionaries bind protocol, request UUID, exact revision, mutator, source version, ordered choices, and deterministic SHA-256 identity;
- local base-36 codes remain ephemeral compression aids;
- compact local choices resolve back to authoritative semantic values before BlueprintDecisionChain mutation;
- stale revision/dictionary reuse fails closed;
- candidate providers cannot widen mutation wire legality because candidate values are dry-run through the authoritative decision chain.

Exact-head validation observed
------------------------------
Validate Continuity Works run 34340512515 completed successfully for exact executable head ee3a0e9b83959810f16256ed05699b02a2bf14be.

Observed successful stages include:
- package installation;
- Python source compilation;
- source-tree agent discovery;
- unit/API contract/PUBLIC_SERVICEABILITY tests, including the new Java 17 hero-spec biome candidate regression;
- production Continuity Works frontend build;
- zero-JavaScript Pages discovery stamping;
- local executable API and public-boundary smoke;
- validation-status publication.

GitHub Pages build/deployment run 34340511569 also completed successfully for the exact same executable head.

Naming cleanup state
--------------------
The retired pre-Continuity public naming cleanup remains closed for active authoritative source and the directly observed generated/deployment path. The exact executable head passed the repository-wide naming guard, production frontend build, rendered/static discovery generation, and GitHub Pages deployment.

A separate browser-executed visual inspection is still not claimed. The whole-source guard plus exact-head production frontend generation and successful Pages deployment are the strongest directly observed acceptance evidence for this run.

Compact decision-chain contract
-------------------------------
The in-process Java compact-blueprint API remains version 1.8.0 and protocol cw-decision-1.

The validated tiny-inference contract remains:
- reference workload: Qwen 2.5 Instruct Q4-class or comparable small local controller;
- hard output ceiling: 64 tokens per inference;
- preferred operating target: 32 tokens or fewer;
- rich decision state remains outside model output;
- one mutation per inference is preferred when dependencies require serial choice;
- upstream changes invalidate only dependent selections;
- raw block placement, NBT/SNBT, commands, operation lists, and primitive geometry remain forbidden at the inference boundary;
- deterministic validation/finalization remain Continuity Works responsibilities.

Net implementation change in this cycle
---------------------------------------
Compared with cycle starting head 41dd27ff5b88c3f5ec14e37060634f4af9bb308a, executable head ee3a0e9b83959810f16256ed05699b02a2bf14be changes exactly:
- modules/continuityworks-api/src/main/java/io/continuityworks/api/blueprint/EraStructureBiomeCandidateSource.java
- modules/continuityworks-api/src/main/java/io/continuityworks/api/blueprint/RoutingBlueprintDecisionCandidateSource.java
- modules/continuityworks-api/src/main/java/io/continuityworks/api/blueprint/ContinuityWorksDecisionAuthorityBootstrap.java
- tests/test_era_structure_biome_candidate_source_java.py

This continuity update modifies only the existing VALIDATION_REPORT.txt authority. No alternate branch, pull request, parallel implementation, duplicate hero vocabulary, workaround layer, or new workflow was introduced.

Public API / deployment boundary
--------------------------------
PUBLIC REMOTE DECISION AUTHORITY: NOT VERIFIED LIVE.

Repository execution now proves that production Java composition can obtain A candidates from the real hero ledger and B candidates from explicit profile labels in the selected real hero specification. GitHub Pages deployment proves the static/public frontend artifact for the exact executable head. No deployed remote Java decision process, production environment binding to that process, external authority-health read-back, or public end-to-end decision sequence against the real Java authority was directly observed. Those remote claims remain open.

Era structure program boundary
------------------------------
E01-001 through E01-017 remain source-developed through hero/build/worldgen stages with production admission still requiring target Minecraft runtime/materialization/placement evidence. E01-018 remains blocked until its authoritative master-catalog archetype label is recovered; no archetype name is to be invented.

Next dependency-valid work
--------------------------
1. Normalize completed hero specifications whose biome/environment sections are prose-only into explicit profile labels while preserving their existing semantics. Do not invent new environmental categories. This is required before B can be a reliable required mutator across every materialized A archetype.
2. Add authoritative C culture candidates only where the selected hero specification exposes explicit machine-safe culture profile labels; normalize existing prose-only culture sections before making C broadly available.
3. Add Q condition and K palette profile sources only where corresponding explicit authoritative option vocabularies exist; do not derive arbitrary choices from narrative prose.
4. Preserve catalog/profile authority fingerprints across persistence/resume so a restored decision cannot reuse a dictionary derived from an older authority snapshot.
5. Remote Java authority attachment remains the highest external integration blocker and stays open until directly observed through the deployed public boundary.
6. Resume hero-structure development at E01-018 only when its authoritative catalog label is recovered.
